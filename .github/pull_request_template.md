## Description
Please describe the changes in this pull request and the rationale behind them.

## Related Issues
Closes # (issue number)

## Quality Checklist
- [ ] My changes pass the local validator: ran `npm run validate` successfully.
- [ ] All new concept directories contain exactly the 8 required files.
- [ ] Frontmatter `id` inside `concept.md` matches the folder name exactly.
- [ ] Metadata `conceptId` inside `questions.js` matches the folder name exactly.
- [ ] No remote resources (like external fonts, styling stylesheets, or tracking APIs) are requested.
- [ ] Touch targets are at least `48px x 48px` where appropriate.
- [ ] Visual elements include read-aloud TTS compatibility tags where appropriate.
