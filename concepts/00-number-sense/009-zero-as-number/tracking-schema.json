{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "Local Progress Tracking Schema",
  "type": "object",
  "properties": {
    "conceptId": { "type": "string", "example": "009-zero-as-number" },
    "attempts": { "type": "integer", "minimum": 0 },
    "bestScore": { "type": "integer", "minimum": 0, "maximum": 10 },
    "lastScore": { "type": "integer", "minimum": 0 },
    "mastered": { "type": "boolean" },
    "lastPracticed": { "type": "string", "format": "date" },
    "mistakePatterns": {
      "type": "array",
      "items": {
        "type": "string",
        "enum": ["overcounts", "after_target", "incorrect_choice"]
      }
    }
  }
}
